# AHPV — Catalogue des Articles (Pack complet configuré)
Ce pack est configuré pour le dépôt **AHPV** (utilisateur **mich59139**).

## Déploiement
1) Crée le dépôt `AHPV` → Upload: `index.html`, `style.css`, `app.js`, dossier `assets/`, dossier `data/`.
2) GitHub Pages → Branch: `main`, dossier **root**.
3) Hard refresh (Ctrl/Cmd+Shift+R).

## Enregistrement GitHub
Clique 🔐 et colle un token (scope: repo). Les constantes en haut de `app.js` pointent vers `mich59139/AHPV`.
